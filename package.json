{
  "name": "vscode-sql-formatter-jp",
  "displayName": "SQL Formatter JP",
  "description": "SQLを整形する。日本人が日本語で細かく設定できることを目的に作成。",
  "version": "0.1.0",
  "publisher": "yo16",
  "repository": {
    "type": "git",
    "url": "https://github.com/googleapis/nodejs-bigquery.git"
  },
  "engines": {
    "vscode": "^1.77.0"
  },
  "categories": [
    "Formatters"
  ],
  "keywords": [
    "sql",
    "formatter",
    "bigquery"
  ],
  "activationEvents": [
    "onCommand:extension.formatSQL",
    "onLanguage:sql"
  ],
  "contributes": {
    "commands": [
      {
        "command": "extension.formatSQL",
        "title": "Format SQL"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "SQL整形",
      "properties": {
        "sql-formatter.データベース": {
          "scope": "resource",
          "type": "string",
          "enum": [
            "sql",
            "bigquery",
            "db2",
            "hive",
            "mariadb",
            "mysql",
            "n1ql",
            "plsql",
            "postgresql",
            "redshift",
            "singlestoredb",
            "snowflake",
            "spark",
            "sqlite",
            "transactsql",
            "tsql",
            "trino"
          ],
          "default": "bigquery",
          "description": "使用するDB。DBによって、SQL dialect(方言)が少し違う。"
        },
        "sql-formatter.字下げのスペース幅": {
          "scope": "resource",
          "type": "number",
          "minimum": 1,
          "default": 4,
          "description": "字下げのスペース数。スペースでなくタブを使いたい場合は、\"字下げにタブを使う\"をチェックオンにする。その場合、この設定値は無効になる。"
        },
        "sql-formatter.字下げにタブを使う": {
          "scope": "resource",
          "type": "boolean",
          "default": false,
          "description": "字下げにタブを使う(チェックオン)か使わない(チェックオフ)か。この設定を使う(チェックオン)にした場合、字下げのスペース数であるtabWdithは無効になる。"
        }
      }
    }
  },
  "main": "./src/extension.js",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{js,jsx,md,json,css,prettierrc,eslintrc}\""
  },
  "devDependencies": {
    "sql-formatter": "^12.2.0",
    "eslint": "^6.6.0",
    "eslint-config-adpyke-es6": "^1.4.13",
    "eslint-config-prettier": "^6.5.0",
    "glob": "^7.1.5",
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "sql-formatter": "^12.2.0"
  }
}
